import React from 'react'
import { StyledCallButton } from 'styles/CallButton.styles'

export const CallButton = () => {
  return (
    <StyledCallButton>
      <button type="button">Start your 7-day FREE trial</button>
    </StyledCallButton>
  )
}