import React from 'react';

export const Register = () => {
  return (
    <form>
      <p>register</p>
    </form>
  );
}

// A main hero-section and the rest of the page with all
// information needed for the user to register and key value preposition